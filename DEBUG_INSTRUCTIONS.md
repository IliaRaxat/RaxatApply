# Инструкции по отладке откликов

## Почему отклики не отправляются?

Важно понимать, что программа работает в три фазы:

1. **Фаза парсинга** - собирает вакансии (отклики НЕ отправляются)
2. **Фаза рейтинга** - сортирует вакансии (отклики НЕ отправляются)  
3. **Фаза отклика** - отправляет отклики (только здесь отправляются отклики!)

## Как проверить работу откликов

### Вариант 1: Использовать тестовый скрипт

```bash
cd backend
npm run debug-apply
```

Этот скрипт:
1. Откроет браузер
2. Попросит войти в аккаунт HH.ru
3. Найдет вакансию без отклика
4. Попробует отправить отклик
5. Покажет результат

### Вариант 2: Запустить основную программу

```bash
# Запустить API сервер (в одном терминале)
npm run api

# Запустить фронтенд (в другом терминале)  
npm run dev

# Открыть http://localhost:3000
# Заполнить токены
# Нажать "Запустить"
```

## Что означают статусы в интерфейсе

- **Парсинг вакансий** - собираем вакансии, отклики НЕ отправляются!
- **Рейтинг вакансий** - сортируем вакансии, отклики НЕ отправляются!
- **Отправка откликов** - только сейчас отправляются отклики!

## Распространенные проблемы

### 1. Кнопка "Откликнуться" не находится
HH.ru часто меняет селекторы. Программа ищет кнопку по нескольким вариантам:
- `[data-qa="vacancy-response-link-top"]`
- `[data-qa="vacancy-response-link-bottom"]` 
- `[data-qa="vacancy-response-link"]`
- По тексту "Откликнуться"

### 2. Отклик не отправляется
Проверьте:
- Есть ли капча на странице?
- Требуется ли подтверждение телефона?
- Не заблокирован ли аккаунт?

### 3. Сопроводительное письмо не заполняется
Программа ищет поля:
- `[data-qa="vacancy-response-letter-text"]`
- `textarea[name="letter"]`
- `[data-qa="vacancy-response-popup-form-letter-input"]`

## Где смотреть логи

Логи выводятся в консоль где запущена программа. Ищите:
- `✅` - успех
- `❌` - ошибка  
- `ℹ️` - информация
- `🔍` - поиск

## Скриншоты

Скриншоты сохраняются в папку `backend/screenshots/` для анализа проблем.